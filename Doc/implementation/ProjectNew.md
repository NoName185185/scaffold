

## Что меняется концептуальноКлючевая мысль: **анонимность теперь реализуется не хешированием, а сокрытием на уровне API-сериализации**. В базе всегда хранится реальный `authorId` (для модерации и разбора нарушений), но при отдаче поста клиенту, если `isAnonymous = true`, поле автора просто не включается в JSON-ответ — кроме случая, когда запрос делает admin.

Обновляю схему БД и роуты под новую концепцию.Теперь добавляю auth-роут (регистрация/вход) и утилиту сериализации, скрывающую автора анонимных постов:Теперь обновляю роуты постов — важнейшая часть: логика сокрытия автора при `isAnonymous`:Обновляю `index.js`, чтобы подключить `attachUser` middleware и новый auth-роут:Добавляю нужные зависимости (`bcryptjs`, `jsonwebtoken`) в `package.json`:Добавляю `JWT_SECRET` в env-пример и обновляю docker-compose:Теперь обновляю фронтенд — добавляю авторизацию и форму создания поста с чекбоксом «постить анонимно»: