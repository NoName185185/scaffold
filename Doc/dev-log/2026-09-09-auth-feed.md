# 2026-09-09 — регистрация, вход, лента

## Сделано

- Модель `User`, миграция `add_user`, сид доски `study` и двух демо-постов
- Один PrismaClient (`src/db.js`)
- `POST /api/auth/register`, `POST /api/auth/login`, `GET /api/auth/me`
- UI: `AuthPanel`, шапка сессии, публичная лента `/study/`

## Решения

- Ленту не закрывали за JWT — просмотр постов без аккаунта
- `Moderator` и `authorHash` не трогали — следующий срез (анонимность / форма поста)

## Открыто

- Форма создания поста, `isAnonymous`, закрытие мутаций
- Vue Router, список досок
